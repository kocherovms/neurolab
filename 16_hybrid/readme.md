**ЦЕЛЬ**

1. Отработать связку CNN+NoncausalTransformer для восприятия визуального поля - прелюдия для разработки агента
2. Проверить гипотезу о быстрой научаемости трансформера при изменении стиля

**ЧТО ДЕЛАЕМ**

1. Датасет из MNIST и Chars74k.
2. MNIST масштабируем до 128х128 - это бьётся с Chars74k и более соответствует будущему визуальному полю агента
3. Разрабатываем модель связку CNN+NoncausalTransformer
4. При тренинге агументация + порча входных данных
5. Цель тренинга: восстановление картинки + классификация (как в `15_transformer/15f_combo_noncausal.ipynb`)
6. Сначала треним на MNIST. Потом скармливаем Chars74k, смотрим что получается, дообучаем. По идее дообучиться должен быстро и на меньшем объеме данных
7. Проблема будет с визуализацией работы по восстановлению. Если в `15_transformer` мы оперировали известными токенами (там было понятно, как их рисовать), то тут видимо придётся ещё какую-то нейросетку прикручивать